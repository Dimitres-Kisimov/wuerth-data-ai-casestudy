# Job → Skill → Evidence map

> **DISCLAIMER.** Independent, **public-info-only** analysis, **not affiliated with Würth**, **no internal Würth data/systems used**. All measured numbers are on **synthetic data** *(labelled)* unless explicitly marked otherwise, and demonstrate method, not Würth outcomes.

Below, each requirement bullet from the two postings is mapped to a concrete piece of evidence: the **repo**, the **file/artifact**, and the **measured number** that backs it up. Where a posting bullet is a general competency (e.g. "team collaboration"), I map it to the artifact that best shows it in practice.

*Updated July 2026.* Since the first version of this document I've finished several logistics and analytics projects — `logistics-flow-studio`, `logistics-digital-twin`, `supply-network-opt`, `market-basket-analysis`, and final measured results in `ml-models-lab` — so the logistics and analytics rows below carry stronger evidence than before. One project, `retail-analytics-real`, is the **real-data** counterpart and is now **completed and published** ([github.com/Dimitres-Kisimov/retail-analytics-real](https://github.com/Dimitres-Kisimov/retail-analytics-real)): UCI Online Retail II, 1,067,371 genuinely messy real transaction rows (2009–2011, CC BY 4.0), with every number measured out-of-sample and labelled *(real data)*. Its headline is deliberately unglamorous — the seasonal-naive baseline **won** the forecast comparison, and I report that plainly, because on real data that's what honesty looks like.

*Second update, July 2026:* three more finished projects are now mapped below. `energy-demand-forecast` (day-ahead load forecasting + a peak-shaving battery LP, methods written from scratch on numpy/scipy) and `quality-anomaly-vision` (surface-defect screening: local statistics vs PCA vs a small conv autoencoder under one pre-registered decision rule) extend the Job #2 analytics evidence — both with the same honest-losses framing as the retail row (in the energy repo, Holt-Winters loses to the naive baseline and the battery does not pay for itself at the assumed tariff; in the vision repo, the pre-registered rule picks PCA over the autoencoder). `quantum-explainer` is a **live, installable** offline PWA ([dimitres-kisimov.github.io/quantum-explainer](https://dimitres-kisimov.github.io/quantum-explainer/)) and backs the Job #1 prototyping/education row.

*Third update, July 2026:* the integration capstone is finished and now leads the Job #2 table. `decision-chain` runs **one real dataset** (UCI Online Retail II, 1,067,371 raw rows, *real data*) through the **whole distributor decision chain** — clean → forecast → replenish → slot → pick → route → cost — and machine-checks **13 cross-stage reconciliation identities**, including the same cleaned revenue reproduced across two repositories **to the penny** (GBP 19,643,861.62). Its honest findings (the naive forecast wins on lumpy demand; the exact slotting optimum is worth only 1.6% over classic ABC; the metaheuristic router beats Clarke-Wright by only 0.2%) are part of the product. `chain-mcp` adds the Job #1 counterpart: an **MCP server** (official `mcp` Python SDK, an open standard) that exposes the real portfolio engines as tools an AI assistant can call mid-conversation. And `logistics-flow-studio` shipped its real-world pass: **bring-your-own CSV data** and a **floor-plan image underlay** with two-point metric calibration, both 100% in-browser.

*Fourth update, August 2026 — reframed around the crown jewels.* This case study now leads with four elevated flagships mapped to concrete Würth opportunities, each embedded with a screenshot in the [README](../README.md): the operable `logistics-flow-studio` (now with a one-click **Story Mode**, an **894-element signature plant** across all 29 object types, and a **user-definable object library**), the OR **engine** `logistics-digital-twin` (its own hand-built slotting / layout SVG visuals), the end-to-end proof `decision-chain` (13 cross-stage identities on one real dataset), and the `distributor-intelligence-platform` **command center** (a `/reconcile` guard that renders a green "no silent drift" verdict over 16/16 cross-engine identities with all 21 headline numbers present). The warehouse and reconciliation rows below carry that new depth.

*Fifth update, August 2026 — a depth pass across the whole portfolio.* Roughly fifteen test-verified features shipped since the previous cut, and the rows below now carry them: a continuous-review **inventory policy** (ROP/EOQ, ABC-XYZ service targets, **EUR 127,421** working capital held to an exact cycle+safety identity) in `distributor-intelligence-platform`; a joint **Monte-Carlo uplift band** (P10–P90 **EUR 145,091–170,723/yr**) with a one-way tornado and a Power BI risk table in `revops-optimizer`; **identity (o)** in `decision-chain` (a forecast error reaches *only* the holding line — the elasticity of total cost equals the holding cost share exactly, **0.0580** on the committed full run); a 7-rule **business-rule validation** layer in `doc-extract-agent` (combined-gate auto-post precision **70.0% → 87.5%**); **pick-path routing** measured against the exact Ratliff–Rosenthal optimum in `logistics-digital-twin`; sales **pacing & target attainment** with an empirical 80% prediction interval — and an honest closed-year back-check the band *missed* — in `sales-kpi-analytics`; **bootstrap skill-score CIs** in `ml-models-lab`; an inventory **service-level frontier** in `supply-network-opt`; a real-data **returns & cancellations** analysis in `retail-analytics-real`; a **champion/challenger retrain harness** with swap-set analysis and gated promotion in the new `fraud-detection-ops`; a category **affinity network** in `market-basket-analysis`; **censored-Weibull age-replacement** policy pricing in the new `predictive-maintenance` (where the calendar rule honestly beats the repo's own detector at the default threshold); a **demand-uncertainty robustness** stress test in `route-optimizer`; a causal **day-ahead dispatch backtest** in `energy-demand-forecast` (72.7% of the perfect-foresight bound captured, with the robust variant a measured negative result); and **SPC monitoring** (p-chart + Western Electric run rules, with a measured calibration correction) in `quality-anomaly-vision`. Two new repos join the registry — `fraud-detection-ops` and `predictive-maintenance` — both measured on **synthetic** data and labelled as such.

*Sixth update, August 2026 — the agentic layer grows teeth, and the twin becomes a factory.* Four capability sets shipped since the previous cut, each verified in its repo's committed docs before being cited here. `logistics-flow-studio` is now **v3.19** (45 headless harnesses, a **112/112** browser self-test, 23 example scenarios) and simulates **factories and process industry**, not only warehouses: **multi-way proportional-flow routing** in the line sim (v3.17 — declared split/merge ratios, flow conservation verified at every node), a **generator-emitted multi-way archetype** plus **RPW line balancing on resolved effective loads** (v3.18), and a **fluids steady-state continuous-flow solver** (v3.19 — pipes/tanks/mixers in m³/h with the bottleneck named and volume conservation verified at every node; "modelled, not measured", NOT CFD). `chain-mcp` now enforces **machine-readable result provenance** on every tool result (**145 tests**): a contract-required provenance block with a canonical data label, the engine repo + commit, and a determinism flag, cross-checked against each tool's prose so the label and the wording cannot disagree. `decision-chain` adds **identity (p) — per-order cost-to-serve**: the ledger spread over all **4,151 real orders** *(real data)* and reassembled **to the cent**, with the concentration measured (top decile **59.0%** of delivered cost, Gini **0.665**). And `agentic-automation-lab` ships a seeded **reliability benchmark** (**1,350 trials**, 51 tests) with a four-way failure-mode taxonomy and retry economics — its headline is the negative result it keeps: **guards catch control-flow faults, but content faults fail silently**, and a bigger retry budget slightly *increases* silent-wrong deliveries.

*Seventh update, August 2026 — each layer now stress-tests its own earlier claim.* Ten test-verified capabilities shipped since the previous cut, each verified in its repo's committed README before being cited here — and the pattern is deliberate: several are corrections or completions of this document's own earlier rows. `doc-extract-agent` prices its gate in euros (an **extraction-error cost model**: auto-posting pays only above a **98.4% precision break-even**, and **the confidence gate alone would lose money** — modeled €181,778/yr vs €173,000 fully manual). `sales-kpi-analytics` adds a **rep performance decomposition** via classic indirect standardization (**92%** of the league-table dispersion is territory, not skill). `market-basket-analysis` prunes its own rule list (**105 of 254 rules — 41% — are redundant**; the 149 kept carry all the information; 68 tests). `distributor-intelligence-platform` measures supplier receipts against the vendor master (**+14.4% safety stock**, and **variability, not lateness, is three quarters of the bill**). `predictive-maintenance` closes the fifth update's loop: **re-optimized economically, the CBM threshold flips the policy ranking** to condition-based — honestly bounded as the shape of the closed loop, not a certified operating point (80 tests). `fraud-detection-ops` simulates the selective-labels feedback loop (**ranking survives label censoring; the probabilities every decision consumes do not**; 55 tests). `revops-optimizer` gates its own recommended price moves on the published Monte-Carlo draws (**17 of 29 accepted — the single biggest move is held**; 69 tests). `retail-analytics-real` adds **lifecycle segmentation** *(real data)*: in the average month **resurrected buyers outnumber month-over-month repeats (439 vs 390)** (113 tests). `supply-network-opt` answers the capacity planner's question (**the lean network has only +8.8% growth headroom**; 71 tests). And `logistics-digital-twin` measures **order batching** on its own exact-optimum yardstick (**−71.3% pick travel — and the routing recommendation flips** from return to largest-gap as tour density rises).

*Eighth update, August 2026 — the levers get priced, the caches get contracts, and each recommendation shows its own bill.* Eight test-verified capabilities shipped since the previous cut, each verified in its repo's committed docs before being cited here. `ml-models-lab` adds an **ablation study** (each model's claimed "one improvement" retrained with the piece knocked out, exact committed deltas — and two knockouts that land inside the bootstrap-CI noise band are honestly *not* counted as wins; numpy models only so the committed table stays bit-reproducible in CI). `energy-demand-forecast` adds the **change-point degree-day decomposition** (the ASHRAE Guideline 14 / PRISM model) with its ground-truth recovery asserted by tests — **weather is 4.5% of the year's energy but 18% of the July peak hour**. `quality-anomaly-vision` answers "the alarm fired — now what?" with a **measured out-of-control action plan** whose headline is a trap: the cheap label-free threshold re-centering quiets the chart while leaving the screen near-blind. `agent-flow-studio` adds a **dry-run cost/latency estimator** whose branch scenarios are proven to match real engine runs branch-for-branch — and which says plainly it is not a bill. `chain-mcp` adds **idempotency + contract-enforced result caching**: every result now says whether it was computed or replayed (**193 tests**). `route-optimizer` adds the **Fleet Size and Mix VRP** with the service price of consolidation shown, not hidden (+25.2% longest route). `decision-chain` adds **identity (q)** — the van-capacity dial swept through the chain, every delivery day's CVRP re-solved per setting, and only the transport line allowed to move. And `bio-efficient-ai` measures the **learned BioHash variant** against its random sibling on the public MNIST benchmark — mid-range wins from a circuit a tenth the size, with the negatives (both tips lost, no memory-frontier point, corpus dependence) reported, not trimmed.

*Ninth update, August 2026 — the silent-fault finding gets its countermeasure.* Two test-verified capabilities shipped since the previous cut, each verified in its repo's committed docs before being cited here. `agentic-automation-lab` ships the **content-verification layer** — the direct countermeasure to the sixth update's own worst number: the business outcome is **independently recomputed from the source input** (re-parse the email, re-resolve every line against the catalog; no answer key, no LLM — the verifier **trusts the tools and distrusts the model**) and `eval/verification.py` replays the reliability benchmark's **exact fault schedule** (same seed, same 1,350 trials; the without-verification arm reproduces the published numbers verbatim, asserted in tests). Because verification converts silent failures into *detected* ones — the only class retries can act on — the anchor tier's delivered-correct rate moves **88.7% → 98.8%** and **silent-wrong quotes fall from 11,523/yr (the committed scorecard's exact count) to 0 on that fault schedule**, at a price stated rather than hidden: human escalations **260 → 1,300/yr**, tokens **~$843 → ~$939/yr** — and the layer's one blind spot (it checks the **structured payload, never the prose**) is **unit-tested rather than hidden**; the suite is now **71 tests** *(synthetic)*. And `quantum-explainer` adds a **superdense coding lesson** — two classical bits through one transmitted qubit via a pre-shared Bell pair, decoded with an honest CNOT+H Bell measurement — with the honesty demos built in: the encoded pair is **locally invisible** (no-signalling made concrete — no faster-than-light communication) and **Holevo's bound is respected, not beaten**; the simulator suite is now **201 physics/behaviour assertions**.

*Tenth update, August 2026 — the twin becomes a plant you can watch work, and every layer prices the decision that follows it.* Fifteen test-verified capabilities shipped since the previous cut, each verified in its repo's committed docs before being cited here. `logistics-flow-studio` reached **v3.24**: four releases (v3.21–v3.24) turned the canvas from a blueprint into a **working plant floor** — poured concrete with 100 mm safety-yellow aisle paint, steel racking and wooden pallets; **living workers** whose pose is a pure function of the station and the sim tick (bending into a rack face, packing at a bench, carrying, scanning, with the load drawn in their hands); **physical goods** that transform across the sim's own stages (pallet-load → carton → tote → parcel) and ride conveyor belts and forklift forks; and **shift realism** (forklifts that drive out and place, congestion bands with frame-rate-invariant anti-strobe hysteresis, dock trailers, an andon lamp) — every layer labelled *rendering, not model*: **no number, no model and no export changes**, and the gates are now **49 harnesses + a 149/149 browser self-test**. `logistics-digital-twin` draws the engine's result **on the floor it happens on** (ABC-classed bays, a real pick tour against its exact optimum, batching before/after). And every analytics layer now prices its own next decision: `doc-extract-agent` traces each value to **real source spans** (derived values honestly marked *no span*); `distributor-intelligence-platform` attributes **every euro of change to a named cause**; `retail-analytics-real` measures **price ladders** on the real rows and refuses to call them elasticity; `revops-optimizer` reconciles two scenarios with an **exact € bridge**; `supply-network-opt` puts a **calendar and a discount rate** on the growth staircase; `predictive-maintenance` prices the **spare parts** its own policy consumes; `sales-kpi-analytics` measures **customer concentration** and finds the company calm but a territory concentrated; `energy-demand-forecast` **sizes the battery** and finds only the first 100 kWh clears break-even; `quality-anomaly-vision` **grades defect severity** and reports that it changes the bill, not the decision; `route-optimizer` models **driver shifts and working time** and reports the statutory limits as slack on this data; `agentic-automation-lab` prices **human-in-the-loop checkpoints per placement**; and `fraud-detection-ops` issues **reason codes** with an exact Shapley decomposition. Two rows below are genuinely new rather than strengthened — human-in-the-loop control of agentic actions (Job #1) and explainability / decision transparency (Job #2) — because until this cut nothing in the portfolio covered either.

---

## Job #1 — Praktikum Data & AI: (Agentic) Automation mit Low-code Plattformen

| Posting requirement | Repo | File / artifact | Proof / measured result *(synthetic)* |
|---------------------|------|-----------------|----------------------------------------|
| Build **AI-agent workflows** (agentic automation) — **and measure how they fail** | `agentic-automation-lab` | agentic tool-use loop modules + the `eval/` benchmark suite (`python eval/reliability.py`, `python eval/verification.py`; byte-stable `eval/reliability_scorecard.md` + `eval/verification_scorecard.md`) + the `src/agentic_lab/verification.py` content-verification layer | Working agentic loops orchestrating multi-step tool calls — with **measured reliability, not vibes**: a seeded fault-injection benchmark re-runs the flows over 9 offline task fixtures (**1,350 trials** — 50 per task across 3 assumed reliability tiers) and scores every run against an answer key into a four-way **failure-mode taxonomy** (success / recovered / detected / **silent**). At the anchor tier (30% assumed fault rate) the RFQ flow measures **76.8% success, 13.6% detected, 9.6% silent** — and the split of *which* faults land where is the finding: **guards catch control-flow faults** (a stuck loop, a hallucinated tool — one even converted into a recovery) **while content faults (a wrong-but-real SKU, a dropped line item) sail through silently**. The **retry economics** are computed, not asserted: at 2 retries the anchor tier delivers **88.7% correct for ~$843/yr in tokens** at the business-case volume — but **~11,500 silent-wrong quotes a year, and no tier in the scenario gets that number to zero** — the quantified argument for the business case's "a rep still reviews every draft" model; under independent retries the cost per *correct* outcome is constant in the retry budget, and a bigger retry budget slightly *increases* silent-wrong deliveries. The per-tier fault rates are **assumed scenario parameters** (anchored to the published ~70% end-to-end figure), **not measured properties of any real model** — the repo says so. And the silent-fault finding now has its **measured countermeasure — the content-verification layer**: the business outcome is **independently recomputed from the source input** (re-parse the email, re-resolve every line against the catalog, re-run the enrichment pipeline; no answer key, no LLM — the trust boundary is explicit: **trust the tools, distrust the model**, exactly the boundary the fault model uses), then `eval/verification.py` replays the reliability benchmark's **exact fault schedule** (same seed, same 3 tiers, same 1,350 trials — the without-verification arm reproduces the published reliability numbers verbatim, asserted in tests). Verification converts silent failures into **detected** ones — the only class retries can act on — so at the anchor tier (30% assumed fault, 2 retries, ~104,000 RFQs/yr) delivered-correct moves **88.7% → 98.8%** and **silent-wrong quotes fall 11,523/yr → 0 on this fault schedule**, at an honest, visible price: human escalations **260 → 1,300/yr**, tokens **~$843 → ~$939/yr** (verification itself adds **0 LLM tokens** — the reference is deterministic code). Honestly bounded: the catch rate is a property of **this fault schedule and this task's recomputable ground truth, not a production guarantee** — and the verifier checks the **structured payload, never the prose** (a lying quote *text* over correct fields verifies clean), a blind spot that is **unit-tested rather than hidden** — which is why the recommendation stays "a rep still reviews every draft": verification shrinks what review has to catch; it doesn't replace it. And the review itself is now **priced where it sits**: `src/agentic_lab/checkpoints.py` adds approval gates that hand out a **resumable, digest-stamped JSON envelope** of the run (transcript, tool state, counters, guard trace, pending calls) so `Agent.resume()` continues from it — nothing is re-sent to the model and no tool runs twice, so **an approved gate costs 0 extra tokens**, where restarting instead of resuming would multiply attempt cost **7.90x**, and an edited envelope is refused rather than resumed. Five placements are measured on the **same fault schedule** at the anchor tier and the business case's ~104,000 RFQs/yr, labour at EUR 45/h: **no gate** leaves **11,523 silent-wrong quotes/yr**; **risk-gated** review — only the runs where guards and verifier disagree — takes that to **0** for 28,349 reviews, **1.24 FTE, EUR 88,948/yr = EUR 7.65 per prevented error**; **pre-commit** EUR 177,205/yr (EUR 15.31); **pre-delivery** EUR 188,438/yr (EUR 16.29) — against the ~EUR 156,000/yr the business case already funds for draft review; and **approving every step** EUR 349,708/yr (EUR 30.28), which **buys nothing extra on this schedule**. Placement also decides what is caught at all: a *skipped validation* is **missed** at `pre_commit` and at `every_step` but **caught** at `pre_delivery` and `risk_gated`, while `wrong_sku` and `dropped_line` are caught at all four gated placements, with **0 false alarms** everywhere. Honestly bounded: **the reviewer is modelled, and modelled generously** — an *ideal* one checking what the gate exposes against the verifier's recompute, so every catch number is an **upper bound** and the zero false-alarm rate **says nothing about people**; review times are assumed scenario parameters except the business case's own 2-minute draft review; there is **no queue model**; and labour in EUR sits beside tokens in USD, **reported side by side, never summed — no FX rate invented**; **100 tests** *(synthetic)* |
| **Agentic integration via an open standard (MCP)** | `chain-mcp` | `chainmcp/` MCP server (official `mcp` Python SDK) + `chainmcp/contract.py` schema layer + `chainmcp/provenance.py` + `chainmcp/cache.py` + Claude Desktop / Claude Code configs | A standard-conformant **Model Context Protocol** server exposing 6 real portfolio engines (forecast, slotting, packing, routing, discount-leakage, repo audit) as AI-callable tools — typed JSON schemas, structured error results that never crash the server, a per-tool honesty label (`data_note`), and **contract-enforced machine-readable result provenance**: every result, success *or* error, carries a provenance block **required by the contract layer's result envelope** (a canonical data label — "synthetic", "real+derived", "real-local", "caller-provided" — the engine repo + commit it was computed from, and an honestly-scoped determinism flag), with the machine label **cross-checked against each tool description's prose so the label and the wording cannot disagree** — an agent can *branch* on what a number was computed on instead of parsing prose. New **idempotency + contract-enforced result caching** (`chainmcp/cache.py`) — identical requests should not recompute, the classic enterprise-integration pattern: every call to a *declared-deterministic* tool gets a reproducible SHA-256 **idempotency key** over a documented material string (server version, tool, canonical defaults-applied arguments, and the **exact engine checkout** — repo, resolved path, commit — so a cached result **can never outlive the code that computed it**), and an identical earlier success is served from a bounded in-process LRU **and says so**: every success result carries `provenance.cache` (cacheable / hit / key), **required by the contract layer's result envelope**, so an agent always knows whether a number was just computed or replayed. Cacheability is **derived from provenance, not asserted separately** (only declared-deterministic tools are cacheable; `portfolio_status` audits live repo state and is *never* cached; error results are never cached), and the scope is honest — one server process, no persistence, no cross-session sharing; **193 tests incl. a live JSON-RPC handshake and an engine-free input-validation matrix**. Data honestly labelled: 5 tools run on deterministic synthetic seeded datasets; `forecast_demand` runs on the real UCI Online Retail II pipeline *(real data; forecasts derived)* |
| **Low-code platforms** (n8n, Power Automate style) | `agentic-automation-lab` | `n8n/rfq_intake_agent.json` | Importable n8n RFQ-intake AI-agent workflow; models **~€625k/yr** |
| **Visual / flow-based** agent building | `agent-flow-studio` | visual agent/flow builder app + `estimator.js` (the **€ Estimate** button) + committed deterministic `docs/FLOW_COST_REPORT.md` (regenerate: `node scripts/estimate-flows.js --write`) | Drag-style flow composition; models **~€47k/yr**. New **dry-run cost/latency estimator**: before a flow ever runs, the graph is priced against a **declared rate card** (illustrative per-token prices — an agent call's prompt is a declared base **plus a declared amount per toolbelt entry**, so fatter toolbelts visibly cost more, plus the trigger text at ~4 chars/token) — per-node tokens/€/ms, **both** latencies (the engine's sequential sum *and* the weighted critical path a parallel executor could reach), the same run priced under every model profile, and, because a Condition takes one branch per run, the **branch scenarios enumerated** with cost as a cheapest → dearest range, reproducing the engine's delivery/skip semantics statically — **the tests prove each scenario's executed set matches a real engine run branch-for-branch**, and a node the engine would skip is priced €0. Honestly framed: **emphatically not a bill** — the shipped agent is a free deterministic mock (its rate-card profile is €0), nothing is measured, and a cyclic flow gets its estimate **withheld, not invented**; the third of three committed deterministic reports (flow lint, data-dependency/provenance, cost), all `--check`-gated in CI; **78 node tests** *(synthetic; declared rates, labelled)* |
| **Connecting systems / APIs** in automations | `agentic-automation-lab` | tool/connector layer in the agent loop | Agents call external tools/APIs as steps in the workflow |
| **Process automation** of high-volume back-office flows | `doc-extract-agent` | RFQ/invoice → structured extraction pipeline + confidence gate; cost model (`python -m eval.run_cost`, `eval/cost_results.json`) | Structured document automation with an auto-post/needs-review confidence gate; models **~€145k/yr** total benefit (of which ~€110k/yr is AP capacity) — a figure the new **extraction-error cost model** now brackets from measured operating points (**€100,556–€152,556/yr** depending on gating policy, at the business case's own modeled rates) rather than asserting |
| **Document intake / understanding** (RFQ, invoice) | `doc-extract-agent` | extraction + **7-rule business-rule validation** layer; eval harness (`python -m eval.run_eval`) + **extraction-error cost model** (`python -m eval.run_cost`) | Semi-structured docs → structured fields, then a structural second gate: **7 field-level business rules** (per-line & totals arithmetic, required fields, date order, IBAN mod-97 checksum, VAT shape). Measured on the 27-document eval set: the confidence gate alone auto-posts at **70.0% precision**; requiring the business rules as well lifts that to **87.5%** — it catches 2 of the 3 silent gate errors, and the one it can't (a currency mis-inferred from prose) is reported, not hidden. The layer "checks format and internal consistency, not truth" — the repo says so. The new **cost model** prices every measured gating policy at the business case's modeled rates (€0.40 pre-filled review, €25 per silent error): **auto-posting a document pays only above 98.4% precision** (break-even 1 − 0.40/25) and no measured policy clears that bar credibly — **the confidence gate alone would lose money on this set** (modeled €181,778/yr vs €173,000 fully manual: 3 silent errors outweigh 10 skipped reviews), the validation layer is worth **≈€109,333/yr in this model**, and the model's actual recommendation is the unglamorous one: **pre-fill everything, auto-post almost nothing** — ~€149,000/yr of the saving is the 4-minute → 45-second pre-fill, not the skipped confirm. Euros modeled, not measured — the repo says so. New **side-by-side original ↔ extracted with real source spans**: the left column stops being an input box once you hit Extract and becomes **the document you submitted, rendered as a line-numbered sheet with every value the engine read underlined where it was read**. That is genuine provenance, not the UI re-searching the text for something that looks like the answer — each extractor records the span of the match that produced its value (`{"start": 177, "end": 190, "line": 12, "col": 17, "text": "INV-2026-8842"}`) — and the counter states coverage plainly: **25 of 26 extracted values located in this document · 1 derived (no span)**. The negatives are built in: **a computed value has no span and says so** ("computed rather than read, so there are no characters in the document to highlight"), **a span that does not match is reported as *not located* rather than drawn somewhere plausible**, and **a span is the source evidence, not a copy of the value** — a `1.234,56` span carries those characters while the caption reads *→ read as 1234.56*. Across **all 30 committed documents** (3 samples + the 27-document eval set) every span must satisfy `text[start:end] == span["text"]`. Honest scope, stated: the sheet is the plain text you submitted — there is no PDF or scan rendering behind it, so **a span is a character range, never a pixel region on an image** |
| **Prototyping** quickly, iterating | `agent-flow-studio`, `agentic-automation-lab`, `logistics-flow-studio` | runnable prototypes / demos | Multiple runnable prototypes; `logistics-flow-studio` (WarehouseTwin **v3.19**) is a full installable **offline WMS twin + factory/plant simulator** (keyword-generated layout with an offline NL parser, WMS-flow simulation with ISO 22400 KPIs, a live material flow + KPI dashboard, automation modelling, an editable standards KB, a one-click Story Mode tour, an 894-element signature plant (29 object types), a user-definable object library, bring-your-own CSV import, a multi-way proportional-flow factory line sim and a fluids steady-state solver; and, since v3.21–v3.24, a plant floor you can watch work — industrial materials, living workers, physical goods and a working shift, all labelled *rendering, not model*; **49 harnesses + a 149/149 self-test**) |
| **Prototyping + explaining a technical topic** (education, hype-free) | `quantum-explainer`, `bio-efficient-ai` | **LIVE:** [dimitres-kisimov.github.io/quantum-explainer](https://dimitres-kisimov.github.io/quantum-explainer/) — installable offline PWA; `bioai/biohash.py` + committed grid `experiments/results/biohash_mnist.csv` | A hand-written state-vector quantum simulator (~300 lines, zero dependencies) with an interactive circuit playground, Bloch sphere, and lessons that state what quantum computers *cannot* do — now through Deutsch's algorithm, Grover's search and **superdense coding**: two classical bits delivered through one transmitted qubit via a pre-shared Bell pair (Alice encodes by touching *only her own qubit*; Bob decodes with the CNOT+H basis rotation — an honest Bell measurement), **mechanism-verified in the suite** (all four 2-bit messages delivered with certainty) with the honesty demos built in — the encoded pair is **locally invisible** (both reduced Bloch arrows at the centre; the phase-flip message's 1000-shot counts identical to the plain Bell pair — **no-signalling made concrete, no faster-than-light communication**) and the scope stated plainly: two qubits are used in total, one shipped ahead as entanglement, so **Holevo's bound is respected, not beaten**. **201 physics/behaviour assertions** pass in plain Node plus a **53-check portable self-test** and **93 structural/offline checks**; no frameworks, no build step, no network calls. Not a data project — it's the evidence that I can prototype a polished tool fast and explain a hard topic to non-experts without hype. `bio-efficient-ai` is the same hype-free discipline aimed at the most over-claimed topic in the field, bio-inspired AI — new **learned-vs-random BioHash study** *(public MNIST benchmark)*: the Ryali et al. (ICML 2020) BioHash — same expand-and-sparsify pipeline as the fly-brain FlyHash, but with the projection *learned* by the Krotov–Hopfield local plasticity rule, no backprop — at an operating point chosen so **learning cannot win by circuit size** (a **10× smaller** expansion, 1,568 vs 15,680 units, making the learned projection *cheaper* than the random one: 1,229,312 vs 1,473,920 MACs/query). The measured read is split three ways and the losses stay in the table: **learning wins the benchmark's mid-range at a discount** (retrieval precision@10 +0.004 to +0.017 at 8–128 hash bits from a tenth the circuit), **the small code space gives the tips back** (it loses below and above the mid-range and saturates outright at L = 1,024: 0.651 < 0.663 — FlyHash's 0.704 tip stays out of reach *by construction*), and **learning does not beat the memory accounting** (classical LSH keeps every memory-frontier point; the learned variant reaches none). Two honest bills stated: training is a one-time ~1.62 × 10¹² MACs per seed, and a learned hash is **data-dependent** — it fits the corpus it trained on, a dependence the random methods simply do not have |
| Show **ROI / business value** of automation | `automation-roi-explorer` | automation ROI dashboard | Models **€383k/yr net**; ROI made explicit and inspectable |
| **Python** engineering for automation | across automation repos | Python source in each repo | Python throughout the agent loops and connectors |
| **Human-in-the-loop control of agentic actions** (approval gates, resumable state, priced per placement) *(new row — previously an acknowledged gap in [`AREAS_FOR_IMPROVEMENT.md`](AREAS_FOR_IMPROVEMENT.md))* | `agentic-automation-lab` | `src/agentic_lab/checkpoints.py` approval gates + `Agent.resume()`; `eval/checkpoints.py` → byte-stable `eval/checkpoint_scorecard.md` / `.csv` + `eval/checkpoint_results.json`; CLI `python -m agentic_lab rfq_intake --checkpoint pre_delivery` | The oversight question an agentic rollout actually has to answer — *where does the human sit, and what does that placement cost?* — answered with a mechanism and a price rather than a policy sentence. The agent stops at a gate and hands out a **resumable, digest-stamped JSON envelope** of its own run; `Agent.resume()` continues on approve/reject, **nothing is re-sent to the model and no tool runs twice**, so an approved gate costs **0 extra tokens** (17,229-byte envelope; restarting instead of resuming would multiply attempt cost **7.90x**), and an envelope edited in a queue is **refused, not resumed**. Five placements measured on the reliability benchmark's own fault schedule (anchor tier, ~104,000 RFQs/yr, labour EUR 45/h): **none** → 11,523 silent-wrong quotes/yr; **risk_gated** (only where guards and verifier disagree) → **0 silent-wrong for 28,349 reviews, 1.24 FTE, EUR 88,948/yr — EUR 7.65 per prevented error**; **pre_commit** EUR 177,205/yr (EUR 15.31); **pre_delivery** EUR 188,438/yr (EUR 16.29); **every_step** EUR 349,708/yr (EUR 30.28) — and per-step approval **buys nothing extra on this schedule**. The negative findings are the point: placement changes *coverage*, not just cost — a **skipped validation is missed at `pre_commit` and at `every_step`** and only caught at `pre_delivery` / `risk_gated`; and **the reviewer is modelled generously** (an ideal one checking the gate's exposure against the deterministic recompute), so every catch rate is an **upper bound** and the **0 false alarms say nothing about people**. Review times are assumed scenario parameters except the business case's 2-minute draft review, there is **no queue model**, and **labour in EUR and tokens in USD are reported side by side, never summed**; a reviewer-attention sensitivity is reported rather than hidden — silent-wrong goes **0 → 2,517 → 6,084/yr** at 100% / 80% / 50% attention; **100 tests** *(synthetic)* |
| Communicate results to **non-technical stakeholders** | `automation-roi-explorer` | ROI dashboard views | Business-readable ROI presentation, not just code |

## Job #2 — Praktikum Data & AI Analytics

| Posting requirement | Repo | File / artifact | Proof / measured result *(synthetic unless noted)* |
|---------------------|------|-----------------|----------------------------------------|
| **One dataset through the whole chain — integration whose numbers reconcile** *(real data + labelled synthetic layers)* | `decision-chain` | committed run artifact `artifacts/full_run.json`; offline Flask **CHAIN DASHBOARD** with the 13-identity reconciliation panel; `cost_drivers.py` + `sensitivity.py` + `order_costs.py` + `fleet_sweep.py` chain consumers; byte-reproducible PDF + Excel deliverables; every identity has a deliberate-corruption FAIL path in the tests | UCI Online Retail II (**1,067,371 raw rows**, *real*) flows through ingest → forecast → inventory → warehouse → transport → costing, and **all 13 machine-checked reconciliation identities PASS**: cleaned revenue reproduced across two repositories **to the penny (GBP 19,643,861.62)**, ledger total to the cent (253,427.16), every pick / carton / route drop conserved (256,787 lines; 70,820 cartons; 4,151 drops). Four additive cross-stage identities extend the suite: **identity (n)** rebuilds the ledger total from the physical drivers the earlier stages booked (253,427.16 == 253,427.16, to the cent); **identity (o) — forecast-error containment** — proves a proportional forecast surge moves **only the holding line** (`total(f) = base_total + (f − 1) × base_holding`), so the elasticity of total cost to a forecast error **equals the holding cost share exactly: 0.0580 (5.80%) on the committed full run** — a forecast error cannot reach the delivered-cost lines of an already-real order book; and **identity (p) — per-order cost-to-serve** — spreads the ledger over the window's real orders by labelled allocation rules (labour by each order's own DES pick minutes, transport by its carton share of its delivery day's CVRP km, facility equally, holding kept honestly on the SKU plane) and machine-checks that the spread **loses nothing and invents nothing**: every order-plane column sums back to its ledger line **to the cent**, both planes jointly to the ledger total **to the cent (253,427.16 == 253,427.16)**, per-order REAL revenue to the window revenue **to the penny**, with exactly one economics row per shipped order — none lost, none phantom, none duplicated. The distribution is the finding: across **4,151 real orders** the costliest decile (416 orders) carries **59.0% of the delivered cost** (Gini **0.665**; median 20.67 vs mean 57.51 GBP) — classic cost-to-serve concentration with the spread itself machine-verified rather than trusted; under the INVENTED rates this describes the **model's shape, never a real order's profitability**, and the repo says so. And **identity (q) — the fleet knob** — the one dial the operational side of this chain actually has: the van capacity (a labelled synthetic-assigned parameter, base 80 cartons) is swept across a deterministic grid (1–320), **every delivery day's CVRP + Clarke-Wright is re-solved at each setting by the existing stage-4b engine** (never a re-invented solver), the ledger is re-priced, and all 13 identities are re-checked at every knob point — (q) itself holds at EVERY capacity: drops and cartons conserved as exact integers (the knob changes *how* cartons ride, never *what* ships), labour / holding / facility / the REAL window revenue invariant **to the cent**, transport == CVRP km × the published rate rebuilt independently to the cent, and the total moves by exactly the transport delta. The measured curve: **capacity buys km only while it binds** (1,686.7 km bought going 40 → 80 cartons vs 194.8 going 160 → 320, on the deterministic fixture path), and Clarke-Wright stays within **1.2%** of the metaheuristic across the whole dial — with one omission deliberate and declared: this ledger prices km, not vehicles, so a bigger van is FREE in the cost model and the curve's downhill direction is a **model property, never fleet advice**; vehicle-days are reported UNPRICED so the service trade-off stays visible without smuggling a new invented rate into the committed ledger. Honest findings are the product: on lumpy demand **nothing beats the naive walk** (MASE 1.782); the exact (Hungarian) slotting optimum is worth only **−1.6% vs classic ABC** (the math — velocity-sorted placement — is explained); OR-Tools CVRP beats 1964 Clarke-Wright by only **−0.2%** and loses 19 of 48 days; the picking crew is **18% utilized** (reported, not hidden); every cost rate is **INVENTED and labelled** on the ledger line — no profit claims |
| **BI / Power BI** dashboards | `revops-optimizer` | `powerbi/DAX_measures.md` + 3-page report pack + disconnected `kpi_uplift_risk` table | DAX measure library + executive report pack; the star schema now also emits a **Power BI risk table** (`kpi_uplift_risk`: the Monte-Carlo **P10/P50/P90** band + VaR/CVaR) with matching DAX measures — no tenant needed to produce or review it, stated honestly in the repo |
| **KPI dashboards / metrics — incl. fair performance measurement** | `sales-kpi-analytics` | KPI metric layer + exec review PDF; pacing bullet chart (`deliverables/pacing_bullet.svg`); rep decomposition (`repperf.py`; `deliverables/rep_performance.svg/.csv`) | Defined KPI set with an executive review deliverable; **sales pacing & target attainment**: the seasonal-naive run-rate projects **€10.91M — 95.7% of the assumed €11.40M plan** (labelled an assumption) with an **empirical 80% prediction interval (€10.77M–€11.05M)** built from the model's own out-of-sample backtest errors — and because the synthetic year has closed, the module shows the **back-check: the realised year landed at €11.28M (98.9% of plan), just *above* the projected band** — an honest miss, reported rather than the band quietly widened. New **rep performance decomposition** — a fair comparison, because volume is not skill: each rep's gap vs an even-share baseline (a stated choice, not a quota) splits into four effects that sum to the gap **exactly** — coverage, frequency, mix, and **execution** via classic **indirect standardization** (actual ÷ mix-adjusted expected; 1.00 = bills what an average rep would with the same book). The honest punch: the #2 rep by raw revenue — the leakage table's top offender — has an execution index of **0.9999** (his +€337k gap is mix +€354.5k, frequency +€198.4k, coverage −€215.8k, execution **−€127** — the book, not the selling); one rep swings #6 → #11 once adjusted; and across all 12 reps **92% of the league-table dispersion is territory (coverage + frequency + mix), only 8% execution**. On this synthetic data (orders assigned at random within region) indices *should* cluster near 1.00 — and they do: the module working, not failing; the SVG's per-rep gap labels are read back and asserted equal to the source to the cent. New **customer concentration & dependency risk** (`saleskpi/concentration.py`) — the risk sitting *inside* the revenue base, measured "with arithmetic rather than prediction": over the trailing 12 months the book scores **Gini 0.5476** and **HHI 68** — ~**148 effective accounts out of 383**, top-10 just **15.4%**, CR-1 3.51% / CR-5 9.33% / CR-10 15.44%, and **156 accounts (41%)** needed to make 80%. "At company level that reads *unconcentrated*, and taken alone it would be reassuring and wrong": the largest account (**C0393, €395,740.85, 3.51% of revenue, 3.47 pp of the assumed plan**) is **35.76% of one rep's own book** (€1,106,637 over 38 customers), whose **HHI is 2,064** — moderately concentrated — with a top-3 of **64.10%** and only **4.8 effective accounts**. "Company-wide the book looks calm (HHI 68, unconcentrated). It is not calm inside every territory." Honestly bounded, and the boundary cost the module a feature: every "what if we lost them" figure is **removal arithmetic on measured revenue**, **no churn probability is modelled anywhere** and no RFM segment is attached to an account — the first cut called the largest, still-ordering account "Churned / dormant", and "the honest fix was to drop the label, not to soften it", with a test asserting no churn/RFM key can ever reappear; the HHI bands are the **DOJ–FTC merger-guideline cuts borrowed as a yardstick — an analogy, not a regulatory reading**, and `gini == 1 − 2 × area` is asserted to six decimals |
| **Uncertainty / risk quantification on business numbers** | `revops-optimizer`, `ml-models-lab`, `sales-kpi-analytics` | `revops/simulate.py` (joint Monte-Carlo + tornado; `deliverables/uplift_distribution.svg`, `sensitivity_tornado.svg`) + `revops/robustness.py` (per-move accept/hold gate; `deliverables/price_move_robustness.csv/.svg`); `mllab/uncertainty.py` + `docs/BENCHMARK_CI.md`; empirical prediction interval in `saleskpi` pacing | A point estimate is not a result: the `revops-optimizer` headline (**EUR 159,966/yr**) gets a joint **Monte-Carlo band — P10–P90 EUR 145,091–170,723/yr**, median (P50) 158,504 — with the honest read that the median lands *below* the point estimate and there is only a **~43% chance of clearing it** (illustrative planning ranges on synthetic data, not a forecast); a one-way **tornado** ranks which assumption moves the number most. New **price-move robustness gate**: the **same fixed-seed draws** behind the band (a test pins the per-draw totals, so this decomposes the published band rather than building a second model) re-examine every recommended price move on three facts — still carried by the assortment MILP in ≥90% of draws, same move direction in ≥80%, and executing the *published* price still positive € at the P10. Verdict on the seeded 256-draw run: **17 of 29 moves ACCEPT (EUR 19,032/yr at baseline), 12 HOLD (EUR 16,187/yr parked** — 10 delist-risk, 2 direction-flips**)** — and the honest headline is that **the single biggest pricing move (+15% on one SKU, EUR 15,836/yr — 45% of the whole pricing uplift) is HELD**, because the SKU survives the assortment re-optimization in only 67% of draws: a point estimate presents that € as bankable; the gate shows it rides on a fragile carry decision. A screening discipline under illustrative ranges, not a guarantee — the repo says so; 69 tests. `ml-models-lab` adds **bootstrap 95% skill-score CIs**: churn skill **+0.457 [+0.381, +0.528]**, elasticity-RMSE skill +0.918 [+0.904, +0.934] — both entirely above zero (the torch models are excluded on purpose: their metrics are not bit-reproducible across builds, and the doc says so). New **scenario compare with an exact € bridge** (`revops/compare.py`; committed `deliverables/plan_compare_bridge.csv` — anchors included, "so the file proves the identity closes"): two plans are solved through the **same** `scenario.solve_plan` core (no second model, no re-derived perturbation) and every euro of the difference is attributed to a **named driver**, summed in **integer cents so it reconciles by construction rather than to a tolerance**. Baseline **€159,966.19** vs *Cost inflation +8%* **€139,487.29** closes with a **residual of exactly €0.00** — and the single −€20,479 delta hides both halves: pricing *gains* **€7,382.88** from changed conditions at frozen prices (+€5.40 of actual price action, −€264.96 assortment mix), promo response loses **€3,707.02** on the carried set, and the **MILP-vs-greedy assortment gap loses €23,895.20** (3 verdict flips; 29 carried SKUs each side, 1 in, 1 out, 28 price-changed). The construction's weakness is disclosed rather than smoothed: **two of the drivers are *ordered*** — splitting a shared SKU's Δ into "the world moved" and "we moved the price" is a sequential attribution, "so it closes to the cent, but a different order would hand the interaction term to the other driver" (action-term rounding bounded at ≤ €0.12/SKU/yr) — and **the assortment gap moves as one line on purpose**, because it is a difference of two optimization objectives over different subsets and "genuinely does not decompose per SKU; I'd rather ship it whole than invent per-SKU drivers". Illustrative planning ranges on synthetic data — "what the model computes under each assumption, not a forecast"; 91 tests |
| **Predictive analytics / forecasting** | `sales-kpi-analytics`, `ml-models-lab` | rolling-origin CV forecasting module; global forecaster | **MASE < 1 vs seasonal-naive** (rolling-origin CV); `ml-models-lab` global forecaster **MASE 0.987 / RMSSE 0.948**, beating both seasonal-naive (1.080 / 1.062) **and** Holt-Winters (1.101 / 1.019) |
| **Forecasting rigour / validation** | `distributor-intelligence-platform` | forecasting engine + `/reconcile` guard | **MASE 0.38** over 9 rolling folds on synthetic series (beats naive baseline); the `/reconcile` executive brief renders a green "no silent drift" verdict over **16/16 cross-engine identities** with all 21 headline numbers present. New **plan-diff station** (`dip/plandiff.py`, `GET`/`POST /api/plan-diff`, dashboard station **ST-07 · Change**) — the question that follows every re-run, *what changed since last time, and why?*: two full plan runs are diffed into a **€ bridge in which every euro is attributed to a named cause**, held up by **11 change identities**. On the shipped default pair the headline moves **€136,972.20 → €115,728.86**, and the single **−€21,243.34** delta hides the actual story: price moves **−€29,931.98** (184 of 200 recommendations moved), SKUs added +€0.00 (0 entered), SKUs dropped **−€150,919.35** (21 left the range) against greedy-baseline re-picks of **+€150,224.01**, routing **+€9,384.00** (+32.64 km saved/run at €1.15/km × 250), and a publication-rounding line of **−€0.02 against a €4.03 bound over 806 values**. The honesty is in the construction: **the rounding line is real, not a plug** — "never fitted to close the gap", and "mis-attribute a single SKU and the bridge stops closing"; the **inventory diff is deliberately kept *out* of the uplift bridge**, so a run pair that changes only the replenishment knobs correctly posts a **€0 uplift delta (tested)**; the assortment lever **moves against intuition, visibly**, and gets its own line instead of being netted to a small unexplained number; and the diff is deterministic, so "routing unchanged" is exactly €0 rather than solver noise, with an infeasible fleet rejected with `400` before the solver is launched. "**The bridge is an identity, not an illustration.**" |
| **Inventory policy / working-capital analytics** | `distributor-intelligence-platform`, `supply-network-opt` | `dip/inventory.py` (continuous-review ROP/EOQ, `GET /api/inventory`) + `dip/reliability.py` (supplier scorecards, `GET /api/reliability`); `supplynet/service_frontier.py` (`deliverables/service_frontier.csv/.svg`) | A textbook **continuous-review (reorder point, order quantity) policy** turns the ABC-XYZ grid into decisions (A/X protected to 98% cycle service, long-tail C/Z to 88%): on the 200-SKU seed it holds **EUR 127,421 inventory working capital** (cycle EUR 111,139 + safety EUR 16,282 — **the identity holds exactly, asserted by tests**) at **5.5x turns** (66 days of cover) and a **99.9% demand-weighted fill rate**; demand modelled normal/independent — "modelled, not measured", and the repo says so. New **supplier lead-time reliability** — measure the receipts, not the vendor master: over 10 suppliers and 2,400 seeded PO receipts (**74.3% on-time** at a stated 2-day grace window), safety stock is re-derived with the textbook variable lead-time formula at the **same** ABC-XYZ service targets — the quoted-basis column reproduces the inventory engine's **EUR 16,282 exactly, SKU by SKU (asserted by tests)**, the measured-basis figure is **EUR 18,626: +EUR 2,344 (+14.4%)**, split exactly into a delay effect (EUR 576) and a variability effect (EUR 1,768) — **the wobble, not the lateness, is three quarters of the bill**, and one supplier that delivers *early* on average (−0.4 days) still costs EUR 542 in buffer. Deliberately kept **out** of the platform's EUR 136,972 uplift total (a working-capital consequence, not an uplift lever); 12 receipts per SKU is a small sample and the receipts are seeded synthetic — the repo says so. `supply-network-opt` adds the **service-level frontier**: going 97.5% → 99.0% costs about **$14,750/yr per service point — roughly 6.3x** the cost per point of the first increment (80% → 85%, $2,353) — the convex "last mile of service is the expensive one" curve, on illustrative unit value / carrying rates *(synthetic)* |
| **Energy forecasting + optimization** | `energy-demand-forecast` | `edf/forecast.py` (3 forecasters from scratch, rolling-origin CV) + `edf/peakshave.py` (peak-shaving LP) + `edf/dispatch.py` (causal backtest; `docs/dispatch_capture.svg`) + `edf/temp_sensitivity.py` (degree-day decomposition; `docs/temp_sensitivity.csv/.svg`); 72 tests | Day-ahead temperature + calendar regression at **MASE 0.497 / MAPE 4.8%**, winning **14 / 14 CV folds** vs seasonal-naive (**1.369 / 17.6%**) — and **Holt-Winters loses to the naive baseline (3.040 / 37.6%), reported, not hidden**. The battery-dispatch LP cuts the mean monthly peak **368.2 → 291.1 kW (−20.9%)** while a fixed evening-timer baseline saves **EUR 0**; worth **~EUR 11,100/yr at an ASSUMED EUR 12/kW-month tariff** — and at an assumed EUR 120k–180k battery cost that is a **10+ year simple payback: the battery does not pay for itself on these assumptions, and the repo's business case says so**. New causal **day-ahead dispatch backtest** (weekly-refit forecast, month-anchored plan, meter-clamped execution): the deployable forecast-driven controller captures **EUR 8,066/yr — 72.7% of the EUR 11,100 perfect-foresight LP bound** (a perfect day-ahead plan reaches 91.2%, so forecast error costs EUR 2,054/yr and the causal one-day horizon the rest); the **robust variant (planning on the 90% conformal band) is a measured negative result that stays in the table — 65.4% capture while cycling the battery 42% harder**, and neither band setting beats the plain point-forecast plan on this site. New **change-point degree-day decomposition** — the utility analyst's other question, *how much of this load is the building and how much is the weather?* — the ASHRAE Guideline 14 / PRISM piecewise-linear model, adapted so an industrial schedule cannot masquerade as weather (168 hour-of-week means absorb the two-shift profile via Frisch–Waugh–Lovell demeaning, pinned against the explicit dummy regression by a test; both balance points grid-searched, not assumed) — and because the meter is synthetic, the attribution is **checkable**: the fit recovers the generator's designed balance points **exactly (cooling 19.0 °C, heating 6.0 °C)** and the heating slope to the digit (3.20 kW/°C), *asserted by the test suite* instead of taken on faith. The finding: **weather-driven load is 68 MWh = 4.5% of the year's energy, yet 18% of the July peak hour (76 kW of 412 kW is chiller load)** — in the worst month the battery is, to first order, shaving the weather; the weather + annual terms explain 68% of the schedule-demeaned load variance. Honestly bounded: **a modelled attribution, not a sub-meter** — a real site would still need sub-metering or an M&V protocol, the occupied/unoccupied split is binary, and the balance points are searched on a 0.5 °C grid; the repo says so. New **battery sizing against the causal controller** (`edf/sizing.py`; `docs/battery_sizing.csv/.svg`) — the question a procurement decision actually turns on, *what size should we buy?*, and it **refuses to answer it from the bound**: every row is a *complete* causal backtest of the deployable controller (weekly-refit forecast, month-anchored plan, meter-clamped execution — every rule unchanged) re-run against a different battery, power scaled at the default spec's 0.30 C. At an **ASSUMED linear EUR 375/kWh installed** (the midpoint of the EUR 120,000–180,000 range) break-even over a 15-year life is **EUR 25 per kWh-year** — and **only the smallest system on the grid clears it**: 100 kWh / 30 kW earns **EUR 3,328/yr (77.0% capture), EUR 33.3/kWh-yr, 11.3-year payback**, against 200 kWh EUR 5,121 (14.6 yr), the default 400 kWh EUR 8,066 (**18.6 yr**), 600 kWh EUR 9,939 (22.6 yr) and 800 kWh EUR 12,579 (23.8 yr): "**The smallest battery on the grid is the best buy, and only it clears the bar.** … Buying four times the battery buys 2.4x the saving and turns an 11-year payback into a 19-year one." The awkward parts stay in the table: the **600→800 kWh step (EUR 13.2/kWh-yr) is worth *more* than the 400→600 step (EUR 9.4)**, so the curve is not concave; and it is an **energy problem, not a power one** — doubling the inverter to 240 kW buys **EUR 0/yr** while halving it to 60 kW costs **EUR 824/yr**. What it does not price is stated: demand charge only (no arbitrage, no TOU stacking), simple undiscounted payback, no degradation or O&M — and "the per-kWh cost is *linear*, which real quotes are not … it flatters exactly the small systems the table just recommended. That caveat cuts against my own headline"; five capacities and two inverter ratings, not a continuous optimum |
| **Python** for analytics | `sales-kpi-analytics`, `revops-optimizer` | analysis + modelling scripts | Python analytics pipelines end-to-end |
| **SQL** querying / data modelling | `sales-kpi-analytics` | SQL query set + spend analysis | Hand-written SQL for KPI and spend analysis |
| **Excel**-level tabular analysis | `sales-kpi-analytics` | spend analysis outputs | Tabular spend breakdowns (Excel-equivalent) |
| **Data modelling** | `revops-optimizer` | optimization data model (assortment/inventory/pricing) | Structured entities feeding MILP / newsvendor / elasticity |
| **Optimization / prescriptive analytics** | `revops-optimizer`, `supply-network-opt`, `logistics-digital-twin` | assortment MILP; facility-location MILP; packing FFD + CP-SAT | MILP > greedy, models **~€160k/yr**; facility-location MILP opens **3 of 8 DCs at −21.2% total cost** vs a greedy baseline ($83,550 on the instance); min-cost flow cross-checked **LP == graph solver to $0.00**; container fill **2.0% → 30.2%** (**56 containers saved**, CP-SAT proves the heuristic optimal on the checked instance) |
| **Elasticity / statistical modelling** | `revops-optimizer`, `ml-models-lab` | price-elasticity + decline-detection layer; elasticity & churn studies; `docs/BENCHMARK_CI.md`; `mllab/ablation.py` → `docs/ABLATION.md` (drift-gated by `tests/test_ablation.py`) | Decline classifier **ROC-AUC 0.99**; `ml-models-lab` elasticity endogeneity bias corrected **+1.52 → +0.03** (profit regret 0.89%); churn **PR-AUC 0.653** with Platt calibration **ECE 0.197 → 0.021**, now with a **bootstrap 95% CI on the skill score: +0.457 [+0.381, +0.528]** — entirely above zero. New **ablation study** — each model's claimed "one principled improvement" retrained with the piece knocked out, through the model's **own** pipeline entry point (no re-implemented training code, so the full-model row reproduces the leaderboard metric exactly and the everything-removed elasticity row *is* the leaderboard's naive-OLS fair baseline): dropping the engineered `freq_slope` feature costs the churn model **−0.247 PR-AUC (0.653 → 0.406)** — on par with removing recency, the classically strongest RFM signal — and dropping the demand-shock control costs the elasticity model **+1.446 RMSE (0.129 → 1.575)**, so **endogeneity handling, not estimator polish, is the load-bearing piece** (shrinkage refines a further +0.031). The honest exception: removing Platt calibration leaves PR-AUC **bit-identical** (a monotone rescaling cannot change a ranking metric) while ECE collapses 0.021 → 0.197 — exactly why the model reports both numbers; and two smaller knockouts (class weighting, the monetary group) land **inside the bootstrap-CI noise band and are not counted as wins** — the report says so. Numpy models only, on purpose: retrained torch metrics are not bit-reproducible across platforms, so committed deltas for them would be machine-dependent |
| **Turning data into business decisions** | `sales-kpi-analytics`, `market-basket-analysis` | discount-leakage analysis + exec PDF; cross-sell recommendations | Surfaces a **€2.6M discount-leakage lever**; cross-sell recommendations from **254 association rules** (top lift **2.41**) across **3 customer segments**, with labelled estimates |
| **Market-basket / cross-sell analytics** | `market-basket-analysis` | Apriori + FP-growth implemented from scratch; `basket/affinity.py` (`deliverables/affinity_network.csv/.svg`); `basket/redundancy.py` (`deliverables/rule_redundancy.csv/.svg`) | **224 frequent itemsets, 254 rules, top lift 2.41**; the two algorithms cross-checked for **exact equality**. New **category affinity network**: 14 categories, 27 lift-weighted edges, split into **3 communities by greedy modularity (weighted modularity 0.58)** with **4 bridge edges between communities** (strongest: electrical–lubricants, lift 1.40) — cross-merchandising candidates. New **rule redundancy pruning** — which rules add information, not repetition: every rule scored by **confidence improvement** (Bayardo, Agrawal & Gunopulos 1999) against the best simpler rule with the same consequent, and the itemset collection classified into **closed and maximal** (Pasquier et al. 1999; Bayardo 1998) — **105 of the 254 rules (41%) are redundant; the remaining 149 carry all of the list's information**, with every pruned rule naming the simpler rule that covers it (a test asserts the covering rule is itself kept); of the 224 itemsets **224 are closed and 126 maximal** — at this scale closure does not compress, the improvement criterion does the pruning work, and the doc says so. Honest notes kept: a pruned rule is not *wrong*, merely already said by a simpler one; on this synthetic data the communities largely rediscover the generator's three archetypes — machinery validation, not a discovery about real customers; 68 tests |
| **Classification / anomaly detection** | `ml-models-lab`, `fraud-detection-ops` | SKU text classifier; autoencoder vs PCA anomaly study; `fdo/` fraud-scoring pipeline | SKU classifier **macro-F1 0.963**; anomaly AE **PR-AUC 0.963 vs PCA 0.951** — a narrow win, honestly reported, with **PCA recommended as the default**. `fraud-detection-ops` (from-scratch NumPy, ~60,000 seeded synthetic transactions at ~1.45% fraud prevalence): **PR-AUC 0.270 / ROC-AUC 0.878** vs a prevalence-random 0.013 baseline and the generator's own **oracle ceiling 0.367** — reported against the ceiling, not just the floor; precision-at-100 **0.40** vs the 1.4% base rate; 78 tests |
| **Model lifecycle / retraining governance (MLOps)** | `fraud-detection-ops` | `fdo/challenger.py` champion/challenger harness (`python -m fdo --challenger`; `figures/champion_challenger.csv`, `challenger_swap.csv`) + `fdo/feedback.py` selective-labels simulation (`python -m fdo --feedback`; `figures/feedback_loop.csv`) | A retrain has to **earn** promotion: the challenger is compared on **swap-set analysis** (what changes at the operating point — swap-out 139 alerts / 4 fraud, swap-in 2 alerts / 0 fraud) and **five pre-declared gates**, all passing on this run, so the measured verdict is **PROMOTE** — at total cost **$8,632 vs $8,841**, a net **$210 (2.4%)** win. The honest read is in the repo: the retrain finds no new fraud (it catches 4 *fewer*), wins purely by shedding analyst load, the margin **rests entirely on the $8/review assumption**, and the ranking is slightly *worse* (0.264 vs 0.270, inside the declared epsilon) — the gate exists precisely so that kind of trade has to be argued, not slipped in. New **selective-labels feedback simulation** — what happens when reviewed alerts become tomorrow's training labels: four labelling-policy arms deployed over three retrain rounds with the threshold and 100-review capacity **frozen**, priced against the ground truth only synthetic data can provide. The measured mechanism is not the one folklore expects: **ranking survives label censoring — final PR-AUC spans just 0.268–0.273 across all four arms** (the clean initial history anchors it) — **but the probabilities die**: the assume-legit arm relabels 292 frauds as legitimate, its calibration degrades to **3.2x the oracle arm's ECE** and its alert volume at the frozen threshold starves **651 → 197** (test recall 61% → 44% while the oracle arm holds 62–67%; a second zero-coverage merchant segment appears) — while its own dashboard reads **100% observed recall every round, by construction**, because the only fraud it knows about is the fraud it reviewed. The model grades its own homework — and every decision layer in the repo (threshold, queue EV) consumes exactly the poisoned probabilities. A model of a process, not a measurement of one — review capacity and the 85% chargeback self-report rate are labelled assumptions, and "ranking survives" is a property of this generator plus a large clean history, not a production promise; the repo says so *(synthetic, labelled)*; 78 tests |
| **Reliability / maintenance analytics (survival modelling)** | `predictive-maintenance` | `pdm/policy.py` — censored Weibull MLE + age-replacement optimization (`docs/policy_comparison.csv/.svg`) + `pdm/cbm_tuning.py` — CBM threshold re-optimization (`docs/cbm_tuning.csv/.svg`) | On the seeded fleet (6 observed failures + 14 right-censored suspensions): **Weibull shape β 4.81, scale η 73.6 days, MTBF 67.4 days, B10 46.1 days**; policy pricing per machine-day — run-to-failure 14.84, **age-replacement 7.16 at T\* = 44.4 days (−51.7% vs run-to-failure)**, condition-based 8.28. The honest headline: **the calendar rule beats the repo's own detector at the default 5%-FPR threshold** — the detector alerts 6/6 failures ≥3 days early, but its false alarms book more than half the policy's cost, and β ≈ 4.8 (failures cluster) is exactly the regime where a calendar rule is hard to beat; the ranking can flip on real fleets, and the repo says so. New **CBM threshold re-optimization — the loop closed**: the 5%-FPR default is a statistical convention nobody chose economically, so an exact sweep re-prices the condition-based policy at **every** candidate threshold (timeliness and false alarms re-measured per threshold, costed with the same Weibull MTBF) — economically tuned (0.262 → 0.397), CBM's cost rate falls **8.28 → 4.02 per machine-day, under the age-replacement optimum's 7.16, and the three-way recommendation flips to condition-based**: the earlier finding wasn't that CBM loses, it was that CBM at a statistically-chosen threshold loses. The sweep also yields a closed-form robustness number — the **break-even inspection cost is 553.3 units, ~11x the assumed 50**, before the calendar rule wins again. Honestly bounded: the tuned threshold is selected on the same held-out machine-days it is then priced on (no second holdout) and the 6/6 timeliness rests on six observed failures — "the flip is the *shape* of the closed loop, not a certified operating point". Detector itself: PCA baseline **ROC-AUC 0.937 / PR-AUC 0.926**, 10/10 machines detected, mean delay 3.4 days (the AE loses and PCA is recommended); CP-SAT crew schedule cuts total weighted delay **23.2%** vs FIFO, proven OPTIMAL. New **spare-parts stocking from the same fitted Weibull** (`pdm/spares.py`; `docs/spares_plan.csv/.svg`) — the bill the policy comparison never sent to the storeroom: every policy is a renewal process consuming one part per cycle, so the fit prices each policy's part-demand rate and a **Poisson base stock** says what 20 machines must hold over 365 days at **95% service**. Run-to-failure and condition-based draw **0.0148 parts/machine-day** (108.3 expected, **base stock 126**, 18 safety); **age replacement at T\* = 44.4 d** shortens the cycle to 43.8 d and draws **0.0229** (166.8 expected, **base stock 188**, 21 safety) — so the calendar rule that wins on cost "draws **+54% more parts** than run-to-failure … while cutting the cost rate 51.7%", a trade-off the earlier row could not see. The Poisson assumption is **checked rather than assumed away**: measured cycle CV is **0.24** and **0.07**, "both well under the 1.00 Poisson assumes", and the renewal-variance alternative would hold **112 / 168** instead — so the base stock is "deliberately **conservative**". Honestly bounded: "**The spares plan is a modelled provisioning exercise, not an order.**" — Poisson assumes independent machines on one pool with **no replenishment lead time, no batching and no repairable-part loop**, the renewal-variance correction is *asymptotic*, and "the layer counts units, not money, so it cannot tell you whether holding 188 spares is worth it"; the CSV header says it too: "SYNTHETIC data; part demand is MODELLED from a censored Weibull fit, not measured". 80 tests — "modelled, not measured" throughout *(synthetic, labelled)* |
| **Explainability / decision transparency** (why did this decision fire?) *(new row — no earlier row covered per-decision attribution)* | `fraud-detection-ops` | `fdo/reasons.py` (`python -m fdo --reasons`); byte-deterministic `figures/reason_code_summary.csv`, `figures/reason_codes.csv`, `figures/reason_codes.svg`; exactness pinned in `tests/test_reasons.py` | A score is not an explanation, and an *approximate* explanation of an exact model is a liability. The reason codes answer "why did this alert fire?" "in the shape regulated lending answers it, a short list of **principal reasons**, computed from the model this repo already trained: **no surrogate model, no sampling, no randomness**". The decomposition is **exact rather than approximate, "which is the only reason it is worth shipping"**: the champion is linear in its standardized features, so fixing a **reference profile** (the mean training-window transaction, which scores **z(r) = −0.509**) makes `z(x) = z(r) + Σⱼ θⱼ(x̃ⱼ − rⱼ)` an identity whose per-feature terms **are** the Shapley values under an interventional reference — and "the tests assert that against brute-force enumeration over all **2^m** coalitions, so the claim is checked in CI rather than cited" (local accuracy to 1e-9, closed form vs brute force to 1e-12, efficiency and group additivity to 1e-12). The alert cut is carried into logit space exactly, so the explained set is **the same 608 alerts the shipped threshold fires, never a re-derived one**. The finding is a negative one and it is the headline: "**The most common reason is not the most predictive one.**" — `merchant_category` is principal on **40.5% (246)** of alerts but confirms fraud only **11.4%** of the time, while `transaction_amount` is principal on **19.1% (116)** and confirms **20.7%**; in the 100-review queue `transaction_amount` heads **59.0%** of rows at **33.0%** confirmed fraud (vs 12.5% across all alerts). Removing just the single largest contribution drops **88.7%** of alerts back under the threshold (median 1 reason, against **3.30** listed per alert, up to four ranked). Read honestly, verbatim: contributions are "**logits against a stated reference**, not probabilities, not dollars, not causal — change the reference and every number changes"; "'reasons to cross' is arithmetic inside the model, not a claim the transaction would have been legitimate"; "a faithful account of the *score* can still be an incomplete account of the *fraud*"; a model-stability check finds **Spearman 0.9997** on calibrated probabilities yet **a different principal reason on 1.5% of alerts**; and "the four-reason format is borrowed from ECOA / Regulation B adverse-action practice as a discipline: a fraud alert is not a credit denial, and **nothing here is legal advice**". Scope: exact **for this model family and this reference** — a gradient-boosted or neural score would need sampling-based attribution with its own error bars *(synthetic, labelled)* |
| **Visual quality inspection (vision anomaly detection)** | `quality-anomaly-vision` | 3 detectors trained on clean images only, one shared scoring rule fixed in advance; metrics from scratch; `qav/spc.py` SPC monitor (`figures/spc_chart.svg`) + `qav/recalibration.py` out-of-control action plan (`figures/recalibration.svg`, `deliverables/recalibration.csv`); 59 tests | Conv autoencoder ROC-AUC **0.779** vs PCA reconstruction **0.772** vs local statistics 0.687 — the AE's lead (0.007) is inside the **pre-registered 0.02 margin, so PCA is recommended over the deep model**; PCA also wins the screening operating point (**TPR 0.407 vs 0.393 at 5% FPR**) and localization (**mean IoU 0.207**). New **SPC monitoring**: a **p-chart** with Phase-I frozen limits (CL 1.31%, UCL 2.07%) under the four classic **Western Electric run rules** — a process shift is caught by the **4-of-5-beyond-1σ** rule while the naked 3σ rule *never fires* in the monitored window, and a camera-drift scenario (true defect rate unchanged) still alarms, correctly read as "investigate the *measurement system*". The calibration correction is measured, not assumed: the cost-optimal threshold's "zero false rejects" on 150 test images **does not survive 1,000 fresh clean parts (0 → 0.70% flagged)** — the test-set zero was measurement resolution, and the repo says so. Honest gaps stated: texture-breaks are hard for every method (best AUC **0.609**, autoencoder only), and training the AE longer made it *worse* (0.779 → 0.738 at 30 epochs). New **measured out-of-control action plan** — "investigate" is not yet an action plan, so four responses to the confirmed camera-drift alarm are measured on the same fresh calibration parts (1,000 clean + 600 defective) at four drift levels and costed with the same illustrative model (in control: **358 EUR/1,000 parts**): doing nothing drowns the line (63% of *all* parts flagged at +0.05, 100% from +0.10 — the robustness section's predicted PCA failure mode, now in EUR); **the cheap fix is a trap the chart cannot see** — re-centering the threshold on the unlabelled stream needs no labels, quiets the p-chart *by construction*, but its ROC-AUC is **bit-identical to doing nothing** (same scores, different cutoff): at +0.10 the "recovered" screen catches **1.1 of 15 defects**, at +0.20 just **0.4** (near-random ranking, AUC 0.538) behind an in-control-looking flag rate — a green chart over a blind screen is the worst monitoring outcome there is, so the action plan must forbid touching the threshold before the measurement system is checked; **re-fitting on 200 recent verified-clean frames genuinely closes the loop** (ROC-AUC back to ~0.785 at every drift level, ~99% of the drift-induced cost recovered at +0.05 and beyond), and repairing the camera stays the root-cause fix (358 EUR). Honest scope: one synthetic drift mode (the one PCA is most fragile to), and the refit window is assumed available, verified clean and free — verification labour, downtime and the risk of refitting on contaminated frames are uncosted; the repo says so. New **defect severity grading** (`qav/severity.py`; `figures/severity.svg`) — not every escape costs the same, so the same threshold sweep runs under a graded ledger: each defective part carries a **measured severity index** (the total absolute intensity its injection displaced, `sum abs(defective − clean)`, recorded at generation time — **no detector involved**), cut at **8** and **15** into **minor 43 parts (29%) / major 72 (48%) / critical 35 (23%)** and priced at an illustrative **10 / 35 / 140 EUR**. Two findings, both kept: "**Grading changes the bill long before it changes the decision.**" — the recommended operating point does **not** move (**score ≥ 0.0217, 0.45% of parts pulled**) because the zero-false-reject cliff pins it, and a critical escape would have to be priced at **259 EUR (7.4× the flat rate)** before the reject rate jumps to 1.83%; and "**The expensive grade is the one the screen sees worst.**" — critical ROC-AUC **0.706** vs **0.819** for major and 0.746 for minor, because **22 of the 35 critical parts are texture-breaks**, the one class no method handles. The bill rises **368 → 509 EUR per 1,000 parts (+39%)**, **63%** of it from **2.3 critical escapes** — and the level-vs-shape check is reported too: re-pricing the same grade *mix* to a mean of 35 EUR gives **340 EUR, 7% *below* the flat model**, so the increase is the shape of the mix, not its level. Honest scope, verbatim: "the cut points and the three prices are **illustrative labelled constants**, the grade **mix** is this generator's mix (a real line's comes from its own defect log), and the index is a synthetic-image proxy for 'how much material the mark disturbs' — not a customer-severity model. What is measured: the index itself, every per-grade detection rate, and every AUC" |
| **Warehouse / intralogistics + factory / process-industry modelling** | `logistics-flow-studio` (WarehouseTwin), `logistics-digital-twin` | warehouse digital twin (offline PWA) — `docs/MEASUREMENTS.md` + `measure_optimizer.js`; CSV importer (`data.js` + `verify_data.js`) + floor-plan underlay; factory line sim + fluids solver (`process.js`, `fluids.js`); slotting + discrete-event sim | One-click layout optimizer **−48.6% pick travel** on the demo layout (pinned + reproducible via `node measure_optimizer.js`); **ABC 80/20 beats random slotting ~21%** on pick travel (A/B comparison); a one-click **Story Mode** cinematic tour, an **894-element signature plant** exercising all 29 object types, and a **user-definable object library** (Siemens Plant-Simulation *UserObjects*-style — define your own object type); **49 headless logic harnesses** + a **149/149** browser self-test; new **factory / process-industry simulation** (v3.17–v3.19): a **multi-way proportional-flow line sim** (declared split ratios that must sum to 1 — an invalid set is rejected with a plain-language message, never guessed — merges, assembly dividing / dismantle multiplying, **flow conservation verified at every node**, a deterministic largest-deficit dispatcher; plain-chain lines stay byte-identical), a **generator-emitted multi-way archetype** ("Machining shop with QA split": a declared 60/40 QA split merging before pack-out — offered 120 parts/hr, bottleneck *QA deep test* at 32 s/unit → **112.5 parts/hr**, line efficiency **≈ 88.1%**, hand-computed) with **RPW line balancing on the resolved per-finished-unit effective loads** (pure-chain output **byte-identical to the legacy balancer, harness-pinned**; over-takt line efficiency bounded to a true 0–100%), and a **fluids steady-state continuous-flow solver** (touching fluid components form a directed network solved analytically in m³/h — the saturated pipe named as the **bottleneck**, tank **overflow horizons** as pure arithmetic (the hand-computable demo fills its tank in **96 min**), **volume conservation verified at every node**, curtailed supply reported as back-pressure) — all labelled a **deterministic steady-state / proportional-flow model, "modelled, not measured": NOT a validated discrete-event simulation, NOT CFD, NOT hydraulics**; **bring-your-own-data**: import your own article/order CSVs 100% in-browser (zero network requests) — your `weekly_picks` become the real demand velocities, imported orders are **replayed exactly**, validation is row-numbered and a failed import changes nothing, and the UI honestly badges "Data: yours" vs "Data: synthetic demo"; **floor-plan image underlay** with **two-point metric calibration** to trace a real hall onto the 1 m grid; imported data and plan images are **excluded from share links by default**; slotting via linear assignment **−44.2% pick travel** (golden-zone A-occupancy **25% → 100%**, reshuffle break-even **~0.7 days**), rendered as the engine's **own** hand-built SVG layout (byte-identical across re-runs, the same velocity palette as the live dashboard); container fill **2.0% → 30.2%** (56 containers saved, CP-SAT optimality proof); hand-rolled discrete-event sim modern-vs-legacy: **cycle time −76.1%, picker travel −66.5%**; new **pick-path routing** (`logitwin/routing.py`): four classic policies (return, s-shape/traversal, midpoint, largest-gap) each measured against a brute-force **exact optimum** (equivalently the **Ratliff–Rosenthal single-block optimum**) — at this low pick density the **return** route is closest (**+3.0%** above optimum on the optimized layout) while s-shape is wasteful (**+7.1%**), the textbook low-density result; and holding the policy fixed, **the velocity-optimized layout routes ~46% shorter than the legacy layout** (return 51.4 m → 27.5 m/order) — the routing-level echo of the slotting story; no heuristic can beat the optimum, verified per order by the test suite; and new **order batching** (`logitwin/batching.py`; committed `docs/batching_comparison.csv/.svg`) — the other canonical picking lever, *how many orders ride on the cart*: FIFO, seed (Elsayed 1981) and Clarke-Wright **savings** batching form the batches, and every batched tour is walked on the **same** aisle geometry against the same **exact optimum** (extended with a Held-Karp dynamic program, cross-checked against the brute force wherever both apply) — on the seeded 101-order shift, **savings batching cuts total pick travel 2,692 → 774 m/shift (−71.3%)** on the optimized layout and −72.3% on legacy, so **batching and slotting stack rather than cancel**; and **batching flips the routing recommendation**: tour density rises 2.9 → 6.0 pick faces and the best executable heuristic moves from return (+3.0% on single orders) to **largest-gap (+1.2% on batched tours)** — the textbook density result, now measured rather than quoted; under exact routing **no batching policy can lose to single-order picking** (a theorem, verified per batch), and with a 1-tote cart every policy collapses **byte-for-byte** to the single-order routing sweep (tested). Travel metres only — tote handling, congestion and the time dimension of batching are stated out of scope. New in `logistics-flow-studio` (**v3.21–v3.24**): the canvas stopped being a blueprint and started reading like a **working plant floor**, shipped as strictly read-only drawing layers. **Industrial material identity** — a poured-concrete slab with deterministic exposed-aggregate speckle and saw-cut control joints on the real ~5 m bay, **100 mm safety-yellow aisle lines** with stencilled travel arrows, 75 mm white zone borders, a 150 mm black/yellow hazard hatch on every dock apron, and **51 element types** re-toned onto real materials (orange-red painted rack uprights on **galvanised steel beams whatever the uprights are painted**, machine gray with safety-orange guards, wooden pallets, kraft cartons; console ink measured at 11.3:1 / 5.3:1, indicator LEDs ≥ 3:1 as non-text UI). **Living workers** — every manned element is staffed by an articulated **1.75 m** figure whose pose is a *pure function of that element and the sim tick*: a picker walks with a real alternating gait and counter-swinging arms, **bends into the rack face** (deep at a floor-level face, barely a lean at a chest-level one) and straightens **with a kraft carton in its hands**; a packer sweeps a tape gun **one-handed** over the bench; a staging worker carries and places; a dock worker **raises a handheld and scans** — in EN ISO 20471-family hi-vis with a deliberately **neutral head: no skin tone, no gender, no identity is modelled**, legible at 7 px, capped at 64 figures. **Physical goods** — a wrapped **EUR pallet-load** off the inbound trailer becomes a **kraft carton** when put-away depalletises it, a **plastic tote** when it is picked and a taped **parcel** when it is packed, the form changing exactly where the sim's own FIFO server does the work; **one MU stays one MU**, so the spawned == in-flight + completed invariant is untouched, and units ride the **belt top**, the **RGV/AGV deck** and the **forklift tines**, follow the belt round the bend and queue **nose-to-tail** along the sim's own route, with rack stock moving through the **existing** deterministic fill pattern — no second inventory model, no new number. **Shift realism** — forklifts take a load and **drive out along the aisle in the direction the sim's own plan routes material** (the haul lane marched against the real layout, so a truck drives an *aisle*, never through a rack), place the pallet and return with the empty, in a loop **continuous in position, heading and fork height over a 4,000-sample sweep**; congestion is an exponential smoother integrated in *sim time* — exactly **frame-rate invariant** — under a **Schmitt trigger** and a **48-tick minimum dwell**, so against a queue crossing its threshold **on every tick for 900 ticks the band changes ZERO times** while the badge keeps the sim's own raw count; **dock trailers** back onto working doors behind a two-second dwell; floor arrows are flipped to agree with the direction material actually goes; and an **andon lamp** reads the run as **shape + colour + words**, never colour alone. All deterministic (no `Date`, no `Math.random`), LOD-gated, resolving to a legible static frame under *prefers-reduced-motion*. **The honesty label is the point:** these are *rendering* corrections — they change **no number, no model and no export** (capacities, KPIs, compliance outcomes, the IFC path and every saved scenario stay byte-identical); the congestion bands are a **drawing filter over the existing synthetic queue heuristic**, the truck / trailer / handling-unit dimensions are **nominal drawing constants**, the one-worker-per-manned-element roster is a drawing heuristic, and none of it is motion capture, ergonomics, a labour standard, a staffing recommendation, a fleet or duty-cycle model, CAD/BIM or a measurement — with what is still missing stated too (a truck does not yet steer round a corner; the docks model no turnaround time and no yard). And in `logistics-digital-twin`, the engine's result is now **drawn on the floor it happens on** — "**a renderer, not a new model**": the assignment comes from the Hungarian optimizer, the geometry from `RoutingGeometry`, the waypoints from `routing.heuristic_route`, and every metre from the routing/batching functions, so **no number on either plate is measured off the drawing** — "it is printed because the engine computed it, and the test suite pins each one". Plate 1 draws six pick aisles on the modelled **5.0 m** pitch, a corner dispatch dock and **6 × 4 bays × 3 levels** shaded by **ABC class**, the A-movers filling the **12 positions nearest the dock (25% → 100%** golden-zone occupancy), and the shift's widest order walked under the recommended `return` policy — **6 pick faces, 69.2 m against the 64.4 m exact optimum for those same picks (+7.5%)**, the heuristic's real gap measured per order rather than asserted. Plate 2 shows the batch that saves most: **orders 44, 76, 87 and 96** cost **60.8 + 63.2 + 63.2 + 66.8 = 254.0 m** walked separately under the *largest-gap* policy, and **66.8 m (−73.7%)** for the same ten pick faces on one **4-tote cart**, both panels under the same routing policy so the difference is batching alone. The drawing conventions are stated **on every plate, because they are drawing and not model**: the model puts every pick face on the aisle centreline and carries no rack side, depth or wall, rack levels are vertical in reality and collapse to one floor point (drawn as three chips across the rack depth), only the two modelled dimensions (5.0 m aisle pitch, 1.2 m bay depth) are true to the scale bar, and the drawn route is always the **executable heuristic** — "an exact optimum is a length, not a drawn route" |
| **Logistics / operations analytics** | `route-optimizer`, `supply-network-opt` | OR-Tools CP-SAT routing + `routeopt/robustness.py` stress test (`deliverables/robustness.csv/.svg`) + `routeopt/fleetmix.py` Fleet Size and Mix (`deliverables/fleet_mix.csv/.svg/.md`); multi-echelon safety stock + service frontier + `supplynet/growth.py` demand-growth capacity plan (`deliverables/growth_plan.csv`, `growth_expansion.svg`) | **4.6% / 31%** routing savings vs baselines (74 tests). New **heterogeneous fleet mix — the Fleet Size and Mix VRP** (Golden, Assad, Levy & Gheysens, 1984): a real distributor shops a *catalogue* of van sizes, so the same OR-Tools engine gets a typed vehicle pool with per-vehicle capacities, per-type EUR/km **and a fixed cost per deployed van** — the objective is money, not kilometres — and the optimizer's mix is scored against every homogeneous fleet under the identical cost model, deterministically (fixed solution limit, byte-identical reruns). Measured on the illustrative labelled catalogue (capacity 25/50/100 at EUR 40/60/90 fixed + EUR 0.70/1.00/1.35 per km): on the 60-customer instance **consolidation wins outright** — 5 large vans (EUR 1,325/day) undercut the status-quo 10 mediums (EUR 1,606) by **17.5%** (distance −35.6%, CO2 −14.9%) — **and the service price is shown, not hidden: the longest route grows +25.2%**; on the 30-customer instance **the mix is genuine** — 1 medium + 2 large (EUR 628/day) beats the best single-size fleet by **8.5%** and the status quo by 14.7%, because the tail of demand does not justify a third big van (197 units riding on 200 units of fleet). Honestly bounded: catalogue costs are illustrative estimates, not quotes; every plan is a heuristic under a fixed search budget, not a proven optimum — on a fixed budget the mixed pool's larger search space can even trail a homogeneous solve, and the deliverable reports whatever the numbers say. Earlier **demand-uncertainty robustness** test: driving each plan through **200 seeded demand scenarios** (±15% noise — a labelled assumption) with detour-to-depot recourse shows zero-buffer plans fail in **~96% of scenarios**; re-planning with just **5% capacity headroom** costs +3.0% planned km but cuts failing scenarios **96% → 44%** and the *expected* day by **4.6%** (1,119 → 1,067 km) — the classic flaw-of-averages result, with the noise level "modelled, not measured" stated in the repo. Safety stock with risk pooling **−65.7%** (network) / **−80.1%** (centralized); service-level frontier convexity **6.3x** ($2,353 → $14,750 per service point). New **demand-growth capacity plan** — the capacity planner's question, answered by the same MILP: a 1.00x–2.00x demand sweep re-solves facility location both unconstrained and with today's DCs pinned open — **the lean 3-DC network has only +8.8% growth headroom** (19,451 units committed capacity vs 17,880 demand; the flip side of the leanness the repo's own N-1 screen flags), and up to that wall freezing the network costs **nothing extra** — capacity, not economics, forces the first change; **the first response to growth is a reshuffle, not a new DC** (at 1.10x the optimizer swaps one DC and stays at 3), the **4th DC first pays at 1.30x** — exactly where any 3-DC design physically caps out — the 5th at 1.65x, and the 6th at 1.95x is an *economic* trigger (a 5-DC design is still feasible there; six is already cheaper); two echelons, two ceilings: the DC pool caps at 2.74x but the plant echelon at only 2.10x, so **plants would bind first**. Uniform growth, deterministic demand and flat rates are stated assumptions — expansion triggers are planning estimates on synthetic data, not forecasts; 71 tests. New **driver shifts & working time** in `route-optimizer` (`routeopt/shifts.py`; `deliverables/driver_shifts.csv/.md/.svg`; `--shift`) — "A van can drive all day; a **driver** cannot": the working day is modelled as three kinds of minute (driving = distance × 60 / speed, service at the kerb, breaks) under a **600-min duty envelope**, a **540-min daily driving limit** and a mandated **45-min break before 270 min** of continuous driving, at 40 km/h and 5 min/stop. The result is a negative finding, reported as found: "**The statutory limits are slack on this data.** … That is the number, so that is what the deliverable says." — audited on the shift-blind savings plan the worst duty is **267 min (4h27)** across 10 vans (**333 min of headroom**), the worst continuous drive is **227 min** against the 270-min threshold (**43 min of slack**), **zero breaks** are required, and **10/10 vans** clear both limits. The cap sweep prices the constraint (uncapped reference **1,001.6 km on 10 vans**): **480 and 420 min cost nothing at all**, 360 → 1,010.3 km, 300 → 1,014.3 km, **240 min +5.3% distance**, **210 min +17.5% and an eleventh van**, and at **180 min there is no plan at any fleet size — "a *proof*, not a search that gave up"**. Honestly bounded: the defaults are "**informed by EU Regulation (EC) No 561/2006** … a *modelling* choice, **not a compliance certification**", "**a model, not a compliance tool**" — a real duty would be longer than every number it reports and "nothing in it should be used to judge whether a real roster is legal"; every capped plan is a heuristic under a fixed search budget, so "the distance curve need not be monotone in the cap"; and the break rule is hand-checked in tests but simply does not fire on this instance. And new in `supply-network-opt`: a **phased build plan** (`supplynet/phasing.py`) puts a **calendar and a discount rate** on the growth staircase — demand grows at an assumed 6%/yr and every year is priced by re-solving the **same** facility MILP through the same `force_open`/`force_closed` pins the resilience and growth modules use. Over 10 years at an assumed 10% discount rate: staging the build costs **$2,750,462 NPV** (open **DC0 in year 2, DC3 in year 6**, ending on **5 DCs**) against a **$2,598,732** free-redesign **lower bound** (7 site closures — "reported as a lower bound" because it closes live DCs at zero cost); **building ahead costs $671,597 more (+24.4%)**, **never closing a site costs $151,730 (+5.8%)**, and **no expansion fails in year 2**, when demand reaches 1.124x and clears the 1.088x wall. Stated assumptions: "the annual growth rate and the discount rate are **illustrative assumptions, not forecasts**", fixed cost is modelled as "a **recurring per-period operating cost, not one-time capex**", a DC opens with "**no construction lead time**", and "the build-ahead premium is the price of readiness rather than a finding that early building is wrong — a model carrying capex, construction lead times or land escalation could reverse the sign" |
| **Real, messy data** *(real data, not synthetic)* | `retail-analytics-real` | UCI Online Retail II (1,067,371 raw rows, CC BY 4.0) cleaning + analytics pipeline; returns analysis (`python -m retail --returns`; `deliverables/returns_analysis.csv`); lifecycle segmentation (`python -m retail --lifecycle`; `deliverables/lifecycle_stages.csv`, `lifecycle_flows.csv`); 113 tests on a committed real-data fixture; QA'd figures; exec PDF + Excel deliverables | **Real data, honestly reported.** Cleaning pipeline logs every step: **94.0% of rows retained**, **22.77% missing CustomerID** flagged (kept for revenue, excluded from customer analytics), **£19,643,862** revenue analyzed. New **returns & cancellations** analysis: returns run at **3.65% of gross value** (GBP 716,426 — leaving GBP 18,927,436 net) and 4.18% of units, across **17,914 return lines in 7,405 cancellation invoices**; because the data carries no credit-note → invoice key, returns are attributed **heuristically** (most recent prior sale of the same StockCode to the same CustomerID) — on that basis **95.0% of returned value matches a prior purchase, at a median of 10 days to return** (p25 4 / p75 30 days) — "a heuristic attribution, not a linked RMA", the repo says so. RFM: **5,852 identified customers, 10 segments** — Champions are **25% of customers carrying 69.0% of identified revenue**. New **lifecycle segmentation & growth accounting** — the operational monthly view: every identified customer gets one stage per calendar month (new / retained / resurrected / at-risk / dormant; the 3-month dormancy threshold is a documented choice *audited against the data* — 78.0% of comebacks land inside it), with the month-to-month flow matrix's structural zeros asserted by tests. Measured over **5,824 customers and 24 complete months**: the average month has 1,041 active buyers = 212 new + 390 retained + 439 resurrected (quick ratio 1.05) — **in a typical month more buyers are *resurrections* than month-over-month repeats (439 vs 390)**; even dormant customers return at **8.9% per month** (29.4% of all resurrections come back from >3 silent months — dormant is not dead here); **31.2% of stage-attributed revenue is customers coming back from a gap**; and the flow matrix independently reproduces the cohort module's month-1 repeat rate (23.3% — two modules, one measured truth). The operations echo of the cohort plateau: for this wholesale-heavy base, skipping months is *normal purchasing behaviour*, and a CRM that treats every one-month absence as churn would misread most of it — stage definitions are a declared lens, not a behavioural truth, and the repo says so. Forecasting (5-fold rolling-origin CV, leakage-safe): **seasonal-naive wins** — MASE **1.094** vs Holt-Winters **1.187** and a lag-features model **1.590** — with only one seasonal cycle of training data. The naive baseline won and I say so. New **price ladders** *(real data)* (`retail/pricing.py`; `deliverables/price_ladder.csv`, the `PriceLadder` sheet, `figures/price_ladder.png`) — "`Price` in this dataset is not a property of a product — it is a property of a *line*": **4,342 of 4,895 SKUs (88.7%) sold at more than one price**, so the ladder is measured before anything is said about pricing. On the cohort with **≥ 4 distinct prices and ≥ 200 invoice lines** (**1,342 SKUs carrying 79.7% of revenue, GBP 15,653,663**; median **6 rungs**, p90/p10 spread **2.00×**), **39.5% of units sold below the posted price, 51.5% at it, 9.0% above** — and **"price realization is 98.6% — and that is not an opportunity"**, the GBP 216,608 gap being 1.4%. Two log-log slopes are "kept apart on purpose": **within week −1.71** — measured on variation that contains **no price change**, "the discount schedule alone" — and **posted price week to week −1.78**, **market-adjusted −1.67** over the **1,113 SKUs** with enough weekly variation, with **96.0%** of market-adjusted slopes negative and **825 of 1,113 (74.1%)** beating their own **199-draw permutation null at p ≤ 0.05** (553 single-price SKUs excluded). The refusal is the finding: "**The honest part is what the slopes do not license.** Read **−1.0 as the benchmark, not zero**" — a buyer who spends the same amount per line whatever the price produces exactly −1 with no demand response at all — the two measurements correlate at only **r = 0.20** per SKU, "this data supports an **assortment-level statement, not a per-SKU price recommendation**", and "prices here were never randomised — there is no experiment, no cost data, no competitor prices, no stock levels, so **nothing here is an elasticity and nothing here is causal**"; "posted price" is itself "a documented choice (modal by lines, ties to the lower rung), not a price list the retailer published" |
| Present findings to **decision-makers** | `revops-optimizer` | executive deck | Exec-level narrative deck built from the analysis |

A note on `logistics-flow-studio`'s standards panel: it references German norms (ASR A1.8, DIN 15185, EN 15512, EPAL/DIN EN 13698, VDI 2510/3564, DGUV) and is explicitly framed as **"aligned to, NOT a certification"** — the same honesty rule as everywhere else in this portfolio. All five passes are now shipped (domain depth with twelve storage systems, material-flow chains, push/pull dynamics, zone/batch/wave picking, the clearly disclaimed illustrative MRO preset, Android packaging scaffold, and the LSP Planner network game), plus the real-world pass: the CSV importer and the floor-plan underlay described in the warehouse row above — and now the factory / process-industry pass (the v3.17–v3.19 multi-way line sim and fluids steady-state solver, likewise described above and labelled "modelled, not measured") and the v3.21–v3.24 plant-floor pass (industrial materials, living workers, physical goods and a working shift — labelled *rendering, not model*: no number, no model and no export changes).

---

### How to read this

- **Repo** = one of my existing portfolio repositories.
- **File / artifact** = the specific thing to open to verify the claim.
- **Proof** = the measured number, on **synthetic data** unless the row says otherwise, always labelled. The one real-data row (`retail-analytics-real`) is measured on real UCI Online Retail II transactions — and its proudest number is the humble one: the seasonal-naive baseline won the forecast comparison, and it's cited because it's true, not because it flatters.

I'd rather under-claim and be reproducible than over-claim. On real Würth data these numbers would change — establishing the real baseline is the first thing I'd do in the internship (see [`AREAS_FOR_IMPROVEMENT.md`](AREAS_FOR_IMPROVEMENT.md)).
